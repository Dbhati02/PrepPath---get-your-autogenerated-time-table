<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>

  <body class="min-h-screen bg-gradient-to-br from-rose-100 via-purple-100 to-sky-100 font-[Quicksand] text-gray-800 overflow-x-hidden">
    <%- include('partials/navbar') %>

    <!-- 🌟 Full Glass Card Layout -->
    <main class="flex justify-center items-center px-4 py-12">
      <div class="w-full max-w-6xl bg-emerald-600 backdrop-blur-xl border border-white/40 rounded-3xl shadow-2xl p-8 flex flex-col lg:flex-row gap-10">

        <!-- 👤 Profile Info Section (LEFT SIDE) -->
        <div class="w-full lg:w-1/2 flex flex-col justify-between space-y-6">
          <!-- User Details Card -->
          <div class="rounded-2xl p-6 shadow-md bg-white/60 backdrop-blur-md border border-white/40 h-full">
            <div class="flex items-center gap-6 mb-6">
              <img src="/images/placeholder.avif" alt="User" class="w-24 h-24 rounded-full border-4 border-gray-300 shadow-lg">
              <div>
                <h1 class="text-3xl font-bold text-rose-600">Hello, <%= user.name %> 🌸</h1>
                <p class="text-base text-gray-600">Let’s keep your journey blooming! 🌿</p>
              </div>
            </div>

            <div class="space-y-4 text-base">
              <p><span class="font-semibold">📧 Email:</span> <%= user.email %></p>
              <p><span class="font-semibold">🗓️ Exam Date:</span> <%= user.examDate.toDateString() %></p>
              <p class="text-xl font-bold text-purple-700">
                <span class="text-2xl">📊 Progress:</span>
                <%= user.overallPercentage ? user.overallPercentage : 0 %>%
              </p>
            </div>
          </div>

          <!-- 🛠️ Action Buttons -->
          <div class="grid sm:grid-cols-2 gap-4">
            <a href="/api/user/updateProfile" class="bg-rose-400 hover:bg-rose-500 text-white py-2 rounded-xl text-center font-semibold shadow-sm transition">
              ✏️ Update Profile
            </a>
            <a href="/api/dailyPlan/todaysPlan" class="bg-green-400 hover:bg-green-500 text-white py-2 rounded-xl text-center font-semibold shadow-sm transition">
              📖 Today's Plan
            </a>
            <a href="/api/dailyPlan/getAllPlans" class="bg-blue-400 hover:bg-blue-500 text-white py-2 rounded-xl text-center font-semibold shadow-sm transition">
              🗓️ Full Timetable
            </a>
            <a href="/api/subject/create" class="bg-yellow-400 hover:bg-yellow-500 text-white py-2 rounded-xl text-center font-semibold shadow-sm transition">
              ➕ Add Subject
            </a>
            <a href="/api/subject/getAll" class="bg-pink-400 hover:bg-pink-500 text-white py-2 rounded-xl text-center font-semibold shadow-sm transition col-span-full">
              📋 View Subjects
            </a>
          </div>
        </div>

        <!-- 🐰 Animal Image Section (RIGHT SIDE) -->
        <div class="w-full lg:w-1/2 flex justify-center items-center">
          <img src="/images/bunny2.png" alt="Cute Animal" class="w-[460px] h-auto object-contain drop-shadow-xl">
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
